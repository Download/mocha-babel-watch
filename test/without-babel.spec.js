describe('mocha --watch "test/**/*.spec.js"', function(){
	it('runs the tests when invoked', function(){
	});

	it('re-runs the tests when some source file changes', function(){
		console.log('Try changing this message to verify that tests are re-run.');
	});
});